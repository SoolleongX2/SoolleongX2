<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.wandukong.soolleongx2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_appicon"
        android:label="술렁술렁"
        android:roundIcon="@mipmap/ic_appicon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SoolleongX2">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".IntroThirdActivity" />
        <activity android:name=".IntroSecondActivity" />
        <activity android:name=".IntroFirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>